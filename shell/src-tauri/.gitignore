# M0 scaffold — verify with cargo check once Rust is installed.
# Rust/Cargo artifacts (never committed)
/target
/gen/schemas

# Local build inputs produced by `cargo tauri icon` and the sidecar build
/icons
/binaries/partner-core-*

*.sig
*.tar.gz
