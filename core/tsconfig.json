{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // noEmit project; '@partner/shared' path-alias pulls in shared/src TS
    // sources, so rootDir must span the workspace root.
    "rootDir": ".."
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
