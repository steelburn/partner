# Staged by stage.sh / stage.ps1 — never committed, and never shared between
# machines (the core bundle embeds nothing secret, but the artifacts are
# platform-specific and the certs definitely are).
core-bundle.cjs
web/
skills-catalog/
secrets/
.env
